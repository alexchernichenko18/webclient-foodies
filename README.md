# Foodies Frontend — Project Setup

This repository contains the frontend part of the **Foodies App** built with React (CRA).  
Below are instructions for running the project locally and configuring environment variables.

---

## 🚀 Getting Started

Create a .env file in the root folder of the project.

```bash
REACT_APP_BASE_URL_API=http://localhost:3000
# REACT_APP_BASE_URL_API=https://foodies-api-vodr.onrender.com
PORT=3001

1. Install dependencies

```bash
npm install

2. Start the development server

```bash
npm start